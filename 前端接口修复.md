当前项目的前端页面已经全部开发完成，UI、路由、页面布局、赛博朋克风格都不要重写。

现在的问题是：前端仍然在使用 mock 数据或假登录逻辑。后端关闭时，前端仍然可以登录，说明前端没有正确访问真实后端接口。

本次任务：按照后端已经提供的所有接口地址，一次性完成 frontend-admin 和 frontend-team 的真实后端接口接入。

修改范围：
- frontend-admin
- frontend-team

不要修改：
- backend
- docs
- 前端原型设计

核心原则：
1. 不重写 UI。
2. 不重写页面样式。
3. 不破坏现有路由。
4. 不改变现有视觉设计。
5. 只重写或修复 api、request、store、页面调用接口的逻辑。
6. 默认必须走真实后端。
7. mock 文件可以保留，但默认绝对不能调用。
8. 后端关闭时，登录必须失败。
9. 后端关闭时，Network 必须能看到 localhost:8080 请求失败。
10. 后端启动且接口返回 code=200 时，才能登录成功。

==================================================
一、后端基础信息
==================================================

后端地址：

http://localhost:8080

统一响应格式：

{
  "code": 200,
  "message": "success",
  "data": {}
}

分页响应格式：

{
  "code": 200,
  "message": "success",
  "data": {
    "records": [],
    "total": 0,
    "page": 1,
    "pageSize": 10
  }
}

前端必须按 code 判断请求是否成功：

- code === 200：成功
- code !== 200：失败，必须抛出错误或显示错误提示
- 请求失败 / 后端关闭：必须失败，不能返回 mock 数据

==================================================
二、环境变量
==================================================

请确认并创建以下文件：

frontend-admin/.env.development

内容：

VITE_API_BASE_URL=http://localhost:8080
VITE_USE_MOCK=false

frontend-team/.env.development

内容：

VITE_API_BASE_URL=http://localhost:8080
VITE_USE_MOCK=false

request.ts 中 baseURL 必须使用：

import.meta.env.VITE_API_BASE_URL || 'http://localhost:8080'

不要写死 mock 地址。
不要写死假 token。
不要接口失败后 fallback 到 mock。

==================================================
三、统一 request.ts 改造
==================================================

请分别修复：

frontend-admin/src/api/request.ts
frontend-team/src/api/request.ts

要求：

1. 使用 axios。
2. baseURL 指向 VITE_API_BASE_URL。
3. timeout 设置为 10000 或合理值。
4. 请求拦截器自动带 token。

管理端 token：
localStorage.getItem('adminToken')

队员端 token：
localStorage.getItem('teamToken')

请求头格式：

Authorization: Bearer ${token}

5. 响应拦截器必须处理统一响应格式。
6. 如果 response.data.code === 200，返回 response.data.data。
7. 如果 code !== 200，必须 Promise.reject 或 throw Error。
8. 如果网络错误、后端关闭、CORS 错误，必须 reject。
9. 不能在 catch 中返回 mock 数据。
10. 不能在 catch 中返回 code=200。
11. 不能自动 fallback 到 mock。
12. 401 时清除 token 并跳回登录页。

==================================================
四、管理端接口接入 frontend-admin
==================================================

请检查并修复以下文件：

frontend-admin/src/api/auth.ts
frontend-admin/src/api/dashboard.ts
frontend-admin/src/api/team.ts
frontend-admin/src/api/member.ts
frontend-admin/src/api/skill.ts
frontend-admin/src/api/timeLog.ts
frontend-admin/src/api/issue.ts
frontend-admin/src/api/export.ts
frontend-admin/src/stores/auth.ts
frontend-admin/src/router/index.ts

如果这些文件不存在，请创建。

------------------------------------
1. 管理员登录
------------------------------------

接口：

POST /api/admin/auth/login

请求：

{
  "username": "admin",
  "password": "admin123456"
}

后端返回 data：

{
  "token": "...",
  "admin": {
    "id": 1,
    "username": "admin",
    "name": "系统管理员",
    "role": "SUPER_ADMIN"
  }
}

前端要求：

1. 只有接口真实返回 token，才允许登录成功。
2. 保存 token 到 localStorage：adminToken。
3. 保存 admin 到 localStorage：adminInfo。
4. 跳转 /admin/dashboard。
5. 后端关闭时必须登录失败。
6. 账号密码错误时必须登录失败。
7. 禁止 mock-admin-token。
8. 禁止 Promise.resolve 假登录。

------------------------------------
2. Dashboard
------------------------------------

接口：

GET /api/admin/dashboard

返回 data：

{
  "summary": {
    "totalTeams": 4,
    "submittedTodayTeams": 3,
    "notSubmittedTodayTeams": 1,
    "totalDisplayMinutes": 1280,
    "pendingIssues": 5
  },
  "teamRanking": [],
  "skillDistribution": [],
  "dailyTrend": [],
  "recentLogs": []
}

要求：

1. Dashboard 页面数据必须来自该接口。
2. 展示时长单位为分钟。
3. 不要显示 h / 小时。
4. 图表数据从 teamRanking、skillDistribution、dailyTrend 获取。
5. 最近提交记录从 recentLogs 获取。

------------------------------------
3. 队伍管理
------------------------------------

接口：

GET /api/admin/teams
POST /api/admin/teams
PUT /api/admin/teams/{id}

查询参数：

keyword
status
page
pageSize

要求：

1. 队伍列表必须来自真实接口。
2. 新增队伍必须调用 POST。
3. 编辑队伍必须调用 PUT。
4. 启用/停用也通过 PUT。
5. 不使用 mock team 数据。

字段兼容：

institutionName / schoolName
teamName
trackName / track
loginCode / inviteCode
status
memberCount
todaySubmitStatus
totalDisplayMinutes

------------------------------------
4. 队员管理
------------------------------------

接口：

GET /api/admin/members
POST /api/admin/members
PUT /api/admin/members/{id}

查询参数：

teamId
keyword
status
page
pageSize

要求：

1. 队员列表来自真实接口。
2. 新增队员调用 POST。
3. 编辑队员调用 PUT。
4. 启用/停用调用 PUT。
5. 队伍筛选项可来自 /api/admin/teams。

------------------------------------
5. 技能树管理
------------------------------------

接口：

GET /api/admin/skills/categories
POST /api/admin/skills/categories
PUT /api/admin/skills/categories/{id}

GET /api/admin/skills/tasks
POST /api/admin/skills/tasks
PUT /api/admin/skills/tasks/{id}

查询参数：

categoryId
status

要求：

1. 左侧模块列表来自 categories 接口。
2. 右侧技能点列表来自 tasks 接口。
3. 新增模块调用 POST categories。
4. 编辑模块调用 PUT categories/{id}。
5. 新增技能点调用 POST tasks。
6. 编辑技能点调用 PUT tasks/{id}。
7. 停用模块/技能点后页面刷新真实数据。
8. 不使用 mock skill 数据。

------------------------------------
6. 展示时长记录管理
------------------------------------

接口：

GET /api/admin/time-logs
POST /api/admin/time-logs/{id}/void

查询参数：

startDate
endDate
teamId
memberId
categoryId
taskId
needSupport
progressStatus
isVoided
page
pageSize

返回字段：

id
recordDate
institutionName
teamName
memberName
categoryName
taskName
durationMinutes
progressStatus
resultDesc
problemDesc
needSupport
isVoided
createdAt

要求：

1. 展示时长记录页面必须来自真实接口。
2. 筛选条件必须真实传给后端。
3. 表格显示 durationMinutes，单位是分钟。
4. 不显示 h。
5. 作废按钮调用：
   POST /api/admin/time-logs/{id}/void
6. 作废请求体：
   {
     "voidReason": "xxx"
   }
7. 操作按钮只能叫“作废”，不能叫删除。
8. 作废成功后重新请求列表。

------------------------------------
7. 问题闭环
------------------------------------

接口：

GET /api/admin/issues
POST /api/admin/issues
PUT /api/admin/issues/{id}

查询参数：

teamId
status
severity
page
pageSize

要求：

1. 问题看板来自真实接口。
2. 新增问题调用 POST。
3. 更新问题状态调用 PUT。
4. 不使用 mock issues 数据。

------------------------------------
8. 数据导出
------------------------------------

接口：

GET /api/admin/export/time-logs

要求：

1. 点击导出展示时长记录时，真实请求该接口。
2. responseType 必须是 blob。
3. 正确触发浏览器下载。
4. 文件名优先从 Content-Disposition 获取。
5. 不使用 mock 提示替代真实下载。

==================================================
五、队员端接口接入 frontend-team
==================================================

请检查并修复以下文件：

frontend-team/src/api/auth.ts
frontend-team/src/api/submit.ts
frontend-team/src/api/skill.ts
frontend-team/src/api/request.ts
frontend-team/src/stores/teamAuth.ts
frontend-team/src/router/index.ts
frontend-team/src/views/TeamLogin.vue
frontend-team/src/views/TeamSubmit.vue
frontend-team/src/views/TeamSuccess.vue

------------------------------------
1. 队员邀请码登录
------------------------------------

接口：

POST /api/team/auth/code-login

请求：

{
  "loginCode": "CQSX0075"
}

返回 data：

{
  "token": "...",
  "team": {
    "id": 1,
    "institutionName": "重庆财经职业学院",
    "teamName": "CQSX-A队",
    "trackName": "新一代信息技术"
  }
}

前端要求：

1. 必须真实请求后端。
2. 只有 code=200 且 data.token 存在时，才能登录成功。
3. 保存 token 到 localStorage：teamToken。
4. 保存 team 信息到 localStorage：teamInfo。
5. 跳转 /team/submit。
6. 后端关闭时登录失败。
7. 邀请码错误时登录失败。
8. 禁止写死 CQSX0075 登录成功。
9. 禁止 mock-team-token。
10. 禁止 Promise.resolve 假登录。

------------------------------------
2. 队员端获取表单选项
------------------------------------

接口：

GET /api/team/form-options

请求头：

Authorization: Bearer teamToken

返回 data：

{
  "team": {},
  "members": [],
  "categories": [
    {
      "id": 1,
      "name": "技能实操",
      "tasks": []
    }
  ]
}

要求：

1. TeamSubmit 页面进入时真实请求该接口。
2. 队员下拉框来自 members。
3. 模块下拉框来自 categories。
4. 技能点下拉框来自当前模块的 tasks。
5. 不使用 mock members/categories/tasks。
6. 如果接口失败，提示加载失败，不允许继续假提交。

------------------------------------
3. 队员端提交展示时长
------------------------------------

接口：

POST /api/team/time-logs

请求体：

{
  "memberId": 1,
  "taskId": 1,
  "recordDate": "2026-05-20",
  "durationMinutes": 8,
  "progressStatus": "已完成",
  "resultDesc": "完成代码展示",
  "problemDesc": "无",
  "needSupport": false
}

要求：

1. 必须真实请求后端。
2. durationMinutes 单位是分钟。
3. 只有后端返回 code=200，才跳转 /team/success。
4. 后端失败时不能跳转成功页。
5. 后端关闭时不能跳转成功页。
6. 禁止 mockSubmit。
7. 禁止 Promise.resolve 假成功。
8. 提交成功后 PostgreSQL 的 time_logs 表必须新增数据。

==================================================
六、彻底关闭 mock
==================================================

请全局搜索并处理：

mock
USE_MOCK
useMock
mockLogin
mockSubmit
mockAdminLogin
mockTeamLogin
mockToken
mock-admin-token
mock-team-token
Promise.resolve
setTimeout
fallback
fake
demo
模拟
假数据

处理规则：

1. mock 文件可以保留。
2. 默认不允许调用 mock。
3. catch 中不能返回 mock 数据。
4. catch 中不能返回成功。
5. import.meta.env.DEV 不能自动启用 mock。
6. VITE_USE_MOCK=false 时绝不能调用 mock。
7. localStorage 不能预置 token。
8. 登录失败不能跳转。
9. 提交失败不能跳转成功页。

==================================================
七、路由守卫
==================================================

管理端：

1. 未登录访问 /admin/dashboard 等页面，跳转 /admin/login。
2. 登录成功后才能进入后台。
3. 401 时清除 adminToken 和 adminInfo，并跳转登录页。

队员端：

1. 未登录访问 /team/submit，跳转 /team/login。
2. 登录成功后才能进入提交页。
3. 401 时清除 teamToken 和 teamInfo，并跳转登录页。

注意：
路由守卫只检查 token 是否存在即可，不要伪造 token。
真实登录必须由后端接口产生 token。

==================================================
八、验收标准
==================================================

修复后必须满足：

测试 A：后端关闭时

1. 清空浏览器 localStorage 和 sessionStorage。
2. 打开 frontend-admin。
3. 输入 admin / admin123456。
4. 必须登录失败。
5. 不能进入 Dashboard。
6. F12 Network 中必须看到请求：
   POST http://localhost:8080/api/admin/auth/login
   并且失败。

7. 打开 frontend-team。
8. 输入 CQSX0075。
9. 必须登录失败。
10. 不能进入提交页。
11. F12 Network 中必须看到请求：
    POST http://localhost:8080/api/team/auth/code-login
    并且失败。

测试 B：后端启动时

1. 启动 backend。
2. 管理端 admin / admin123456 登录成功。
3. 队员端 CQSX0075 登录成功。
4. 队员端进入提交页时，真实请求 GET /api/team/form-options。
5. 队员端提交 8 分钟展示时长时，真实请求 POST /api/team/time-logs。
6. PostgreSQL 的 time_logs 表必须新增一条记录。
7. 管理端展示时长记录页面能看到这条记录。
8. Dashboard 能显示真实统计。
9. 管理端作废记录后，数据库 is_voided=true。
10. Excel 导出真实下载。

==================================================
九、构建检查
==================================================

完成后执行：

cd frontend-admin
npm run build

cd ../frontend-team
npm run build

如果构建失败，请继续修复直到成功。